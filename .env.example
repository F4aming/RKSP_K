JWT_SECRET=super-secret-key
JWT_EXPIRES_IN=7d

# SMTP (optional — without it verification codes are logged in backend)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@parking.local
